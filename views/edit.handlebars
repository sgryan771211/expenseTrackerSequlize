<div class="container mt-5">
  <div class="row">
    <div class="col-6 mx-auto">
      <h4>請修改你的支出</h4>
      <span>*必填欄位</span>

      <form action="/record/{{{record.id}}}?_method=PUT" method="POST">

        <div class="form-group">
          <label for="name">名稱*:</label>
          <input type="text" placeholder="name" id="name" name="name" value="{{{record.name}}}" class="form-control">
        </div>

        <div class="form-group">
          <label for="date">日期*:</label>
          <input type="date" placeholder="date" id="date" name="date" value="{{{record.date}}}" class="form-control">
        </div>

        <div class="form-group">
          <label for="category">類別*:</label>
          <select type="text" id="category" name="category" class="form-control">
            <option value="home">家居物業</option>
            <option value="shuttle-van">交通出行</option>
            <option value="grin-beam">休閒娛樂</option>
            <option value="utensils">餐飲食品</option>
            <option value="pen">其他</option>
          </select>
        </div>

        <div class="form-group">
          <label for="amount">金額*:</label>
          <input type="text" placeholder="amount" id="amount" name="amount" value="{{{record.amount}}}"
            class="form-control">
        </div>

        <div class="modal-footer">
          <button type="button submit" class="btn btn-primary ml-3">修改支出</button>
        </div>
      </form>
    </div>
  </div>
</div>